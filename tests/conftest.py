"""Pytest fixtures shared across the test tree.

M0 baseline: only houses the minimal smoke fixture set. The full testcontainers
+ factory_boy + agency singleton fixture set lands in PR 3 alongside the
hardening test layer.
"""

from __future__ import annotations
